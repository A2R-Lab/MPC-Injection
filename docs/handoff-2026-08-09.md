# Handoff — 2026-08-09

## Workspace and current intent

- Repository: `/home/roy/MPC-RL`.
- The user paused the schema-v4 recovery campaign and has **not** explicitly approved Gate V4.5. Do not resume final evaluation or open the sealed final-test seeds unless the user clearly asks to continue.
- The worktree is intentionally dirty and contains substantial user/session changes. Preserve all existing changes; do not revert or overwrite unrelated files.
- Never inspect, enumerate, or modify `deploy/robots/go2/config/policy/velocity/policies/`.

## Authoritative state and artifacts

- Recovery workflow and gate state: `/home/roy/MPC-RL/docs/go2_barrel_roll_v4_recovery_plan.md`.
- Campaign validation: `/home/roy/MPC-RL/logs/go2_barrel_roll_v4/campaign/campaign_validation.json`.
- Gate V4.5 review report: `/home/roy/MPC-RL/logs/go2_barrel_roll_v4/campaign/validation_review/report.json`.
- `logs/go2_barrel_roll_v4/campaign/final_evaluation/` is absent as of this handoff. The final evaluation has not started.
- The user said the review policies “look pretty good” but also said to discuss them later and not continue. Treat that as feedback, not approval.
- Detailed G9/v4 history, metrics, selected checkpoints, hashes, and gate evidence already live in the files above; read them instead of reconstructing the campaign from this handoff.

## Schema-v4 0% baseline capability added

- Training launcher: `/home/roy/MPC-RL/mpc_rl/run_go2_barrel_roll_v4_0pct.py`.
  - Accepts only `percentage=0` and delegates every other frozen v4 option check to the existing training implementation.
  - Records its own source hash in run provenance.
  - Writes `COMPLETE` only after verifying the recorded MPC replay percentage range and final value were exactly `0.0`.
- Best-policy video CLI: `/home/roy/MPC-RL/mpc_rl/evaluate_go2_barrel_roll_v4_0pct.py`.
  - Loads the success-selected `best_model`.
  - Defaults to ten non-scored rollout seeds `7000000`–`7000009`, disjoint from v4 validation and sealed final-test ranges.
- Regression tests: `/home/roy/MPC-RL/tests/test_go2_barrel_roll_v4_0pct.py`.
- Full training/video/trajectory/Viser command chain: `/home/roy/MPC-RL/README.md`, schema-v4 0% section.
- No 0% training, videos, or trajectories were launched.
- `mpc_rl/train.py` was deliberately left unchanged by this addition because it is campaign-hash-locked. Its SHA-256 remained `7c157506978516af03b5773be0ecd04a7bdcb8bfce35f8697109f273a063c978`, matching `campaign_validation.json`.

## Most recent 25% video request

- The user requested commands only, with no execution, to record ten additional videos from each 25%-injection best checkpoint (training seeds 1 and 2), regardless of success.
- The response supplied a single Bash/Python heredoc using `load_saved_model_for_video_eval` and `evaluate_and_record`, with consecutive rollout seeds `9000000`–`9000009`.
- Intended output directories were:
  - `logs/go2_barrel_roll_v4_extra_videos/seed1_best_9000000_9000009`
  - `logs/go2_barrel_roll_v4_extra_videos/seed2_best_9000000_9000009`
- The command was not run and no output was created by the agent.

## Validation status

- New focused tests: `5 passed`.
- Broad project suite excluding two unrelated broken test inputs: `185 passed, 1 skipped`.
- Remaining relevant sysID tests excluding the missing-report assertion: `13 passed`.
- Unfiltered `pytest -q tests`: `198 passed, 1 skipped`, plus:
  - error in `tests/gen_traj_data_test.py` because fixture `data_dir` is undefined;
  - failure in `tests/test_go2_sysid.py` because `deploy/sys_id/report.html` is absent.
- Repository-root test discovery additionally fails in vendored `deps/mujoco_mpc` because generated `filter_pb2` is unavailable.
- Syntax compilation and `git diff --check` passed for the new files.

## Safe continuation

1. Follow the user’s newest request literally. If they ask only for commands, do not run them.
2. Do not interpret visual approval casually. Before any sealed final evaluation, require an explicit instruction to continue/approve and follow the write-once procedure in the v4 recovery plan.
3. If asked to run the 0% baseline, use the documented launcher rather than editing `mpc_rl/train.py`.
4. Preserve the current campaign directory and all dirty-worktree changes.

## Suggested skills

- No domain-specific robotics skill is available or required for ordinary continuation.
- Invoke `$grill-me` only if the user explicitly asks to stress-test the experiment or evaluation plan.
- Invoke `$handoff` again when transferring the session to another agent.
